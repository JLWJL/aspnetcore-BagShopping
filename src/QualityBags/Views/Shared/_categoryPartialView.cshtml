@model IEnumerable<QualityBags.Models.Category>


    @if (Model.Count() >0)
    {
        @foreach (var cat in Model)
        {
            <li><a asp-area="" asp-controller="Home" asp-action="Index" asp-route-catFilter="@cat.CategoryID">@cat.CategoryName</a></li>
        }

    }

